machine:
  node:
    version: 8.5.0
  timezone: Asia/Tokyo
dependencies:
  override:
    - yarn
test:
  override:
    - yarn test
deployment:
  production:
    branch: master
    commands:
      - yarn global add surge && yarn build && surge --project ./dist --domain movieing-astronaut-coming-soon.surge.sh

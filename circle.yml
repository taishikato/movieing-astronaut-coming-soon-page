machine:
  node:
    version: 7.10
  timezone: Asia/Tokyo
deployment:
  production:
    branch: master
    commands:
      - npm run build && surge --project ./dist --domain movieing-astronaut-coming-soon.surge.sh
